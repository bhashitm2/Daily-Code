double power(double b, int e) {
double ans=1.00;
        int n=abs(a);
        int m=a;
        while(n>0){
            if(n%2==1){
                ans = double(ans*x);
                cout<<ans<<" ";
                n--;
            }
            else{
                x*=x;
                n=n/2;
                cout<<ans<<" ";
            }
        }
        if(m<0)return 1/ans;
        return ans;
}
